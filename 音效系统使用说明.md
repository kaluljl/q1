# 🎵 音效系统使用说明

## 📌 系统概览

游戏已集成完整的音频系统，包括：
- ✅ 背景音乐播放
- ✅ 音效播放（程序化音调）
- ✅ 音量控制
- ✅ 音频设置界面
- ✅ 按钮点击音效示例

## 🎮 当前功能

### 1. 音效已启用 ✅
游戏启动时会自动初始化音频系统。默认状态：
- 🎵 背景音乐：**开启**（音量70%）
- 🔊 音效：**开启**（音量80%）

### 2. 程序化音效 ✅
由于没有音频文件，系统使用 `ToneGenerator` 生成音调：
- 按钮点击：短促的嘟声
- 建造成功：积极的提示音
- 建造失败：错误提示音
- 获得金币：清脆的双音
- 警告：警报音
- ...等17种不同音效

### 3. 音频设置 ✅
在游戏中访问：**主界面 → 设置 ⚙️ → 音频 🎵**

可以调整：
- 背景音乐开关 🎵
- 音效开关 🔊
- 背景音乐音量滑块（0-100%）
- 音效音量滑块（0-100%）

设置会自动保存，下次启动游戏时恢复。

## 🔊 已集成的音效

### 底部导航栏
所有按钮都会播放点击音效：
- 🏗️ 建筑
- 📋 任务
- 💭 心声
- 📜 政策
- 👥 市民
- 🎨 主题

## 📝 开发者指南

### 如何在代码中播放音效

#### 方法1：直接调用
```kotlin
import com.citysimulator.game.audio.SoundManager
import com.citysimulator.game.audio.SoundType

// 播放按钮点击音效
SoundManager.playSound(SoundType.BUTTON_CLICK)

// 播放建造成功音效
SoundManager.playSound(SoundType.BUILD_SUCCESS)

// 播放金币获得音效
SoundManager.playSound(SoundType.COIN_EARN)
```

#### 方法2：使用扩展函数
```kotlin
import com.citysimulator.game.audio.*

// 在建造成功时
playBuildSuccessSound()

// 在获得金币时
playCoinEarnSound()

// 在警告时
playWarningSound()
```

#### 方法3：在Compose UI中使用
```kotlin
import com.citysimulator.game.audio.clickableWithSound

Button(
    modifier = Modifier.clickableWithSound(SoundType.BUTTON_CLICK) {
        // 你的点击处理逻辑
    }
) {
    Text("点击我")
}
```

### 可用的音效类型

```kotlin
enum class SoundType {
    // UI音效
    BUTTON_CLICK,       // 按钮点击
    MENU_OPEN,          // 菜单打开
    MENU_CLOSE,         // 菜单关闭
    
    // 建造音效
    BUILD_SUCCESS,      // 建造成功
    BUILD_FAIL,         // 建造失败
    DEMOLISH,           // 拆除建筑
    
    // 经济音效
    COIN_EARN,          // 获得金币
    COIN_SPEND,         // 消费金币
    
    // 事件音效
    LEVEL_UP,           // 升级
    ACHIEVEMENT,        // 成就达成
    WARNING,            // 警告
    ERROR,              // 错误
    
    // 市民音效
    CITIZEN_HAPPY,      // 市民开心
    CITIZEN_SAD,        // 市民不满
    
    // 特殊音效
    WONDER_COMPLETE,    // 奇观完成
    SEASON_CHANGE,      // 季节变化
}
```

## 🎼 背景音乐播放

### 播放背景音乐
```kotlin
// 假设你在 res/raw/ 中有一个 main_theme.mp3
SoundManager.playMusic(context, R.raw.main_theme, loop = true)
```

### 控制背景音乐
```kotlin
// 暂停
SoundManager.pauseMusic()

// 恢复
SoundManager.resumeMusic()

// 停止
SoundManager.stopMusic()

// 调整音量（0.0 - 1.0）
SoundManager.setMusicVolume(0.5f)
```

## 📥 添加真实音效文件

### 步骤1：下载音效
参考 `免费音效资源指南.md`，从以下网站下载：
- https://freesound.org/
- https://www.zapsplat.com/
- https://mixkit.co/free-sound-effects/

### 步骤2：准备文件
- 文件格式：MP3 或 OGG
- 文件命名：小写字母+下划线（例如：`button_click.mp3`）
- 文件位置：`CitySimulatorAndroid/app/src/main/res/raw/`

### 步骤3：更新代码
在 `SoundManager.kt` 中找到 `loadSoundEffects()` 函数：

```kotlin
private fun loadSoundEffects(context: Context) {
    try {
        // 取消注释并添加你的音效
        soundMap[SoundType.BUTTON_CLICK] = soundPool?.load(context, R.raw.button_click, 1) ?: 0
        soundMap[SoundType.BUILD_SUCCESS] = soundPool?.load(context, R.raw.build_success, 1) ?: 0
        soundMap[SoundType.COIN_EARN] = soundPool?.load(context, R.raw.coin_earn, 1) ?: 0
        // ... 添加更多音效
        
        Log.d(TAG, "✅ 音效加载完成：${soundMap.size} 个")
    } catch (e: Exception) {
        Log.e(TAG, "❌ 音效加载失败", e)
    }
}
```

### 步骤4：启用音效加载
在 `SoundManager.kt` 的 `initialize()` 函数中：

```kotlin
fun initialize(context: Context) {
    // ... 其他初始化代码
    
    // 取消这行的注释
    loadSoundEffects(context)
    
    // ...
}
```

### 步骤5：重新编译
```bash
cd CitySimulatorAndroid
.\gradlew.bat assembleDebug
```

## 🎯 推荐的音效添加顺序

1. **优先级高**（最常用）：
   - ✅ `button_click.mp3` - 按钮点击（已用ToneGenerator）
   - `coin_earn.mp3` - 获得金币
   - `build_success.mp3` - 建造成功

2. **优先级中**（经常用）：
   - `warning.mp3` - 警告提示
   - `error.mp3` - 错误提示
   - `menu_open.mp3` - 菜单打开

3. **优先级低**（偶尔用）：
   - `wonder_complete.mp3` - 奇观完成
   - `level_up.mp3` - 升级
   - `season_change.mp3` - 季节变化

## 🔍 调试和测试

### 查看音效日志
在 Android Studio 的 Logcat 中过滤 `SoundManager` 或 `TonePlayer`：

```
🎵 初始化音频系统
✅ 音频系统初始化完成
🔊 播放音效：BUTTON_CLICK
🎵 背景音乐：开启
🔊 音效：开启
```

### 测试音效
1. 打开游戏
2. 点击任何底部导航栏按钮
3. 你应该听到短促的提示音

### 测试音量控制
1. 进入 **设置** → **音频**
2. 调整音量滑块
3. 点击按钮测试音量变化
4. 关闭音效开关，确认音效停止

## ⚠️ 常见问题

### Q: 为什么听不到音效？
A: 检查以下项：
1. 手机音量是否开启
2. 游戏内音效开关是否开启（设置→音频）
3. 是否有音效文件（没有则使用ToneGenerator）

### Q: 音效太响/太小？
A: 进入 **设置 → 音频**，调整音效音量滑块。

### Q: 程序化音效太简陋？
A: 这是临时方案。请按照上述步骤添加真实音效文件。

### Q: 如何完全禁用音效？
A: 进入 **设置 → 音频**，关闭背景音乐和音效开关。

## 🚀 性能提示

1. **音效文件大小**：单个文件建议 < 100KB
2. **同时播放数**：SoundPool限制为10个流
3. **背景音乐**：建议文件大小 < 3MB
4. **音效缓存**：音效会在启动时预加载到内存

## 📊 当前系统状态

| 功能 | 状态 | 说明 |
|------|------|------|
| 音效系统 | ✅ 已实现 | 使用ToneGenerator |
| 背景音乐 | ✅ 已实现 | 需要添加音乐文件 |
| 音量控制 | ✅ 已实现 | 独立控制音乐/音效 |
| 设置持久化 | ✅ 已实现 | SharedPreferences |
| UI集成 | ✅ 部分完成 | 底部栏已集成 |
| 真实音效 | ❌ 待添加 | 需下载音效文件 |

## 📚 相关文件

- `SoundManager.kt` - 音频管理核心
- `TonePlayer.kt` - 程序化音调生成
- `SoundExtensions.kt` - 便捷扩展函数
- `AudioSettingsPanel.kt` - 音频设置UI
- `免费音效资源指南.md` - 音效资源推荐

---

**提示**：游戏已经可以正常播放程序化音效了！如果想要更好的音质，只需下载音效文件并放入 `res/raw/` 目录即可。🎉

